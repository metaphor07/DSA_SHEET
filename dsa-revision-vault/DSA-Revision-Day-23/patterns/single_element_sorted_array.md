# Pattern: Single Element in Sorted Array

Before single element:
```text
pairs start at even index
```

After single element:
```text
pairs start at odd index
```

Memory hook:
```text
Single element breaks the even-odd pair pattern.
```
